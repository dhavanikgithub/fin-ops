# ===========================================
# Environment Configuration Template
# ===========================================
# This is a template file for environment variables.
# Copy this file to .env and update the values according to your setup.
# 
# IMPORTANT: Never commit your actual .env file to version control!
# The .env file should contain your real credentials and be kept private.
#
# Usage:
# 1. Copy this file: cp .env-template .env
# 2. Edit .env with your actual values
# 3. Keep .env in your .gitignore file
# ===========================================

# Database Configuration
# PostgreSQL database connection settings
DB_HOST=localhost                    # Database host (usually localhost for development)
DB_PORT=5432                        # PostgreSQL default port
DB_NAME=your_database_name          # Name of your database (e.g., finance_inventory)
DB_USER=your_database_user          # Database username (e.g., postgres)
DB_PASSWORD=your_database_password  # Database password (keep this secure!)

# Environment Configuration
# Determines the application environment and behavior
ENVIRONMENT=staging             # Options: staging, production

# Server Configuration
# Application server settings
PORT=3001                          # Port number for the server to listen on

# ===========================================
# Additional Notes:
# - Ensure PostgreSQL is installed and running
# - Create the database before starting the application
# - Use strong passwords for production environments
# - Consider using environment-specific databases
# ==========================================