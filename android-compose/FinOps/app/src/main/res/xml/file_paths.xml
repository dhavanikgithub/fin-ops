<?xml version="1.0" encoding="utf-8"?>
<paths>
    <!-- External files directory for Downloads -->
    <external-files-path
        name="downloads"
        path="Download/" />

    <!-- Cache directory for temporary files -->
    <cache-path
        name="cache"
        path="/" />

    <!-- External cache for temporary sharing -->
    <external-cache-path
        name="external_cache"
        path="/" />
</paths>
